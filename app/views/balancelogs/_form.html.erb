<%= form_for(@balancelog) do |f| %>
  <% if @balancelog.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@balancelog.errors.count, "error") %> prohibited this balancelog from being saved:</h2>

      <ul>
      <% @balancelog.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cart_id %><br>
    <%= f.number_field :cart_id %>
  </div>
  <div class="field">
    <%= f.label :nominal %><br>
    <%= f.number_field :nominal %>
  </div>
  <div class="field">
    <%= f.label :saldo %><br>
    <%= f.number_field :saldo %>
  </div>
  <div class="field">
    <%= f.label :keterangan %><br>
    <%= f.text_area :keterangan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
